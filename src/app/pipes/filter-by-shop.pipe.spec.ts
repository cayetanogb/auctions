import { FilterByShopPipe } from './filter-by-shop.pipe';

describe('FilterByShopPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByShopPipe();
    expect(pipe).toBeTruthy();
  });
});
